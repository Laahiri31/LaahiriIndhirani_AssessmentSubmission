# Party Menu UI Redesign - TODO

## Step 1: Theme foundation
- [ ] Update `src/styles/common.css` to match provided dark premium palette, typography, radii, shadows.

## Step 2: Login page UI
- [ ] Update `src/styles/signin.css` to premium glass centered login card.
- [ ] Update `src/pages/SignIn.jsx` markup with classNames only (no logic changes) to match design.

## Step 3: Menu page UI
- [ ] Update `src/styles/menu.css` for dark header, filter container, pills, search bar, 3/2/1 grid responsiveness.
- [ ] Update `src/components/FilterBar.jsx` markup/CSS hooks for orange search button (no state logic changes).
- [ ] Ensure `src/components/FoodCard.jsx` visuals match spec (mostly CSS).

## Step 4: Food detail page UI
